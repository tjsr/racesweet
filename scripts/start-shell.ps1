# For an AI-Native shell
Start-AIShell

# For modern cross-platform PowerShell 7 Core
# Start-Process "pwsh.exe" -ArgumentList "-NoExit"

# Alternative: For legacy Windows PowerShell (if pwsh isn't installed)
# Start-Process "powershell.exe" -ArgumentList "-NoExit"
